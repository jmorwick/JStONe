from setuptools import setup, find_packages

setup(
    name='jstone',
    version='0.1',
    description='A library for cannonicalizing and hashing a JSON document and its components.',
    author='Joseph Kendall-Morwick',
    packages=find_packages(),
)
